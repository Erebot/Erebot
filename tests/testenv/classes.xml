<project name="TestEnv">
    <property name="interfaces"><![CDATA[
        abstract class ErebotModuleBase {
            const RELOAD_INIT       = 0x01;
            const RELOAD_TESTING    = 0x02;
            const RELOAD_MEMBERS    = 0x10;
            const RELOAD_HANDLERS   = 0x20;
            const RELOAD_ALL        = 0xF0;

            public function getTranslator()
            {
                return new ErebotStubbedI18n('foo', 'bar');
            }
        }

        interface iErebotI18n {}
        interface iErebotConnection {}
        interface iErebotMainConfig {}
        interface iErebotNetworkConfig {}
        interface iErebotServerConfig {}
        interface iErebot {}

        class EErebot extends Exception {}
        class EErebotInvalidValue extends EErebot {}
        class EErebotNotFound extends EErebot {}
    ]]></property>
    <php expression="${interfaces}"/>
</project>
