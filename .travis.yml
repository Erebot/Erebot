language: php

php:
    - 5.3.2
    - 5.3
    - 5.4

branches:
    only: master

notifications:
    email: false
    irc: "irc.iiens.net#Erebot"

before_script:
    - pyrus install pear/Console_CommandLine
    - pyrus install pear/File_Gettext-beta
    - phpenv rehash

script: phpunit --bootstrap tests/testenv/bootstrap.php

