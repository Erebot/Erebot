language: php

php:
    - 5.2
    - 5.3.2
    - 5.3
    - 5.4

branches:
    only: master

notifications:
    email: false
    irc: "irc.iiens.net#Erebot"

before_script:
    - sudo apt-get install gettext
    - pyrus install pear/Console_CommandLine
    - pyrus install pear/File_Gettext
    - phpenv rehash

script: phpunit --bootstrap tests/testenv/bootstrap.php

